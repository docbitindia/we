import React,{useState}from'react';import{ArrowRight,LockKeyhole,Mail,UserRound}from'lucide-react';import{navigate}from'../router/useRoute';import{useAuth}from'../context/AuthContext';
export function AuthPanel(){const{signIn,signUp,loading,configured}=useAuth();const[mode,setMode]=useState<'login'|'signup'>('signup');const[name,setName]=useState('');const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[busy,setBusy]=useState(false);const[error,setError]=useState('');const submit=async(e:React.FormEvent)=>{e.preventDefault();setError('');setBusy(true);try{if(!configured)throw new Error('Authentication is not configured on this deployment.');if(mode==='signup')await signUp(name,email,password);else await signIn(email,password);navigate('/workspace')}catch(x){setError(x instanceof Error?x.message:'Unable to continue.')}finally{setBusy(false)}};return <div className="hero-auth-card"><div className="flex rounded-xl bg-slate-100 p-1"><button type="button" onClick={()=>setMode('signup')} className={`flex-1 rounded-lg py-2 text-xs font-bold ${mode==='signup'?'bg-white text-slate-900 shadow-sm':'text-slate-500'}`}>Create account</button><button type="button" onClick={()=>setMode('login')} className={`flex-1 rounded-lg py-2 text-xs font-bold ${mode==='login'?'bg-white text-slate-900 shadow-sm':'text-slate-500'}`}>Log in</button></div><div className="mt-5"><p className="text-[10px] font-bold uppercase tracking-[.14em] text-blue-600">{mode==='signup'?'7-day free trial':'Welcome back'}</p><h2 className="mt-1 text-lg font-bold text-slate-950">{mode==='signup'?'Start your DocBit workspace':'Continue to your workspace'}</h2></div><form onSubmit={submit} className="mt-4 grid gap-2.5">{mode==='signup'&&<label className="relative"><UserRound className="auth-field-icon" size={15}/><input className="hero-auth-input" placeholder="Your name" value={name} onChange={e=>setName(e.target.value)} required/></label>}<label className="relative"><Mail className="auth-field-icon" size={15}/><input className="hero-auth-input" type="email" placeholder="Email address" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label className="relative"><LockKeyhole className="auth-field-icon" size={15}/><input className="hero-auth-input" type="password" placeholder="Password" value={password} onChange={e=>setPassword(e.target.value)} minLength={8} required/></label>{error&&<p className="text-[10px] leading-4 text-rose-600">{error}</p>}<button disabled={busy||loading} className="btn-primary mt-1 w-full justify-center">{busy?'Please wait…':mode==='signup'?'Start free trial':'Log in'} <ArrowRight size={15}/></button></form><p className="mt-3 text-center text-[9px] text-slate-400">No card required. Your trial status is stored with the account.</p></div>}
