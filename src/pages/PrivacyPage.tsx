import React from 'react';
import { Header } from '../components/Header';
import { Footer } from '../components/Footer';
import { usePageMeta } from '../hooks/usePageMeta';
export function PrivacyPage(){usePageMeta({path:'/privacy',title:'Privacy — DocBit',description:'How DocBit handles authentication and uploaded files.'});return <div className="min-h-screen bg-white"><Header/><main className="mx-auto max-w-4xl px-5 py-16 sm:px-8 lg:py-24"><p className="eyebrow">Privacy</p><h1 className="public-title">Your data should have a clear boundary.</h1><p className="public-lead">DocBit is designed to distinguish local processing from cloud persistence. DocBit keeps the core editing workflow focused on the signed-in account and the files the user chooses to process.</p><div className="prose-doc"><h2>Local processing</h2><p>Common Excel, CSV and JSON parsing and transformation can happen in your browser, which avoids unnecessary server transfers.</p><h2>Files</h2><p>Files are processed locally where practical. Any future server-side storage should be limited to data the user explicitly chooses to retain and protected with authentication and Row Level Security.</p><h2>Authentication</h2><p>Account authentication is handled through Supabase Auth. Server-side administrative credentials are never intended to be exposed to the browser.</p><p className="text-xs text-slate-400">Replace this product-level privacy summary with your final legal privacy notice before launch.</p></div></main><Footer/></div>}
